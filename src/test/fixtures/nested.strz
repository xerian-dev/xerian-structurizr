workspace "Nested" {
  model {
    system = softwareSystem "System" {
      web = container "Web App"
      db = container "Database"
    }
  }
  views {
    container system "Containers" {
      include *
    }
  }
}
